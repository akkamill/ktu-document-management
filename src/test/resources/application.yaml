spring:
  datasource:
    url: jdbc:tc:postgresql:16-alpine:///ktu_docs_test
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver
  jpa:
    hibernate:
      ddl-auto: create-drop
  liquibase:
    enabled: true
app:
  storage:
    upload-dir: ./test-uploads